// import { create } from "zustand";

// interface Calculater {
//   result: number;
//   setCalculator: (value: number) => void;
// }
// export const useyorestore = create<Calculater>((set) => ({
//   result: 0,
//   setCalculator: (val) => {
//     set((state) => ({ result: state.val }));
//   },
// }));
